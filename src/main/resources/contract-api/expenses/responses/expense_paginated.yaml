components:
  schemas:
    ExpensePaginatedResponse:
      type: object
      properties:
        content:
          type: array
          items:
            $ref: './expense.yaml#/components/schemas/ExpenseResponse'
        size:
          type: integer
          format: int32
        totalElements:
          type: integer
          format: int32