components:
  schemas:
    Budget:
      type: object
      properties:
        id:
          type: string
        name:
          type: string
        amount:
          $ref: '../../commons/schemas/money.yaml#/components/schemas/Money'
        startDate:
          type: string
          format: date
        endDate:
          type: string
          format: date