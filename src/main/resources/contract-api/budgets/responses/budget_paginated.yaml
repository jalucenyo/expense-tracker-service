components:
  schemas:
    BudgetPaginated:
      type: object
      properties:
        content:
          type: array
          items:
            $ref: '../schemas/budget.yaml#/components/schemas/Budget'
        size:
          type: integer
          format: int32
        totalElements:
          type: integer
          format: int32