components:
  schemas:
    BudgetPaginatedResponse:
      type: object
      properties:
        content:
          type: array
          items:
            $ref: '../responses/budget.yaml#/components/schemas/BudgetResponse'
        size:
          type: integer
          format: int32
        totalElements:
          type: integer
          format: int32